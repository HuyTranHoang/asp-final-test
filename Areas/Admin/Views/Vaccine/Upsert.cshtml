@model VaccineDto

@{
    ViewData["Title"] =  Model.Vaccine.Id != 0 ? "Update" : "Create" + " Vaccine";
}

<section class="bg-body-tertiary mt-4 pb-3 rounded-bottom shadow-lg">
    <h1 class="bg-body-secondary bg-gradient px-3 py-4 rounded-top text-center">
        @ViewBag.Title
    </h1>

    <div class="d-flex justify-content-end px-4 py-2">
        <a asp-action="Index" class="btn btn-primary text-decoration-none text-white">
            <i class="fa-arrow-left-to-line fa-regular"></i> Back to vaccine list
        </a>
    </div>

    <div class="p-4">
        <form asp-action="Upsert" asp-route-id="@Model.Vaccine.Id">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Vaccine.Id" />
            <div class="form-group mb-3">
                <label asp-for="Vaccine.Name" class="control-label"></label>
                <input asp-for="Vaccine.Name" class="form-control" />
                <span asp-validation-for="Vaccine.Name" class="text-danger"></span>
            </div>
            <div class="form-group mb-3">
                <label asp-for="Vaccine.ManufacturingCountry" class="control-label"></label>
                <input asp-for="Vaccine.ManufacturingCountry" class="form-control" />
                <span asp-validation-for="Vaccine.ManufacturingCountry" class="text-danger"></span>
            </div>
            <div class="form-group mb-3">
                <label asp-for="Vaccine.ExpirationDate" class="control-label"></label>
                <input asp-for="Vaccine.ExpirationDate" class="form-control" />
                <span asp-validation-for="Vaccine.ExpirationDate" class="text-danger"></span>
            </div>
            <div class="form-group mb-3">
                <label asp-for="Vaccine.Price" class="control-label"></label>
                <input asp-for="Vaccine.Price" class="form-control" />
                <span asp-validation-for="Vaccine.Price" class="text-danger"></span>
            </div>
            <div class="form-group mb-3">
                <label asp-for="Vaccine.CategoryId" class="control-label"></label>
                <select asp-for="Vaccine.CategoryId" class="form-control" asp-items="Model.CategoryListItem">
                    <option value="">-- Select category --</option>
                </select>
                <span asp-validation-for="Vaccine.CategoryId" class="text-danger"></span>
            </div>
            <div class="d-flex form-group justify-content-center mt-3">
                <input class="btn btn-primary text-white" style="width: 10rem" type="submit" value="@(Model.Vaccine.Id != 0 ? "Update" : "Create")" />
            </div>
        </form>
    </div>
</section>


@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}